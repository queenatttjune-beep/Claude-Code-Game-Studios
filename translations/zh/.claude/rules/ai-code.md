---
paths:
  - "src/ai/**"
---

# AI 代码规则

- AI 更新预算：每帧最多 2ms — 通过性能分析验证
- 所有 AI 参数必须可通过数据文件调节（行为树权重、感知范围、计时器）
- AI 必须可调试：为所有 AI 状态实现可视化 hook（路径、感知锥体、决策树）
- AI 应预示意图 — 玩家需要时间阅读和做出反应
- 优先使用基于效用或行为树的方法，而非硬编码的 if/else 链
- 群体 AI 必须支持编队、侧翼包抄和基于数据的角色分配
- 所有 AI 状态机必须记录状态转换以便调试
- 绝不信任来自网络且未经验证的 AI 输入
