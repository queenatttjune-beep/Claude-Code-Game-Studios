---
paths:
  - "src/ui/**"
---

# UI 代码规则

- UI 绝不能拥有或直接修改游戏状态 — 仅显示，使用命令/事件请求更改
- 所有 UI 文本必须通过本地化系统 — 无面向用户的硬编码字符串
- 所有交互元素同时支持键盘/鼠标和手柄输入
- 所有动画必须可跳过，并尊重用户运动/无障碍偏好
- UI 声音通过音频事件系统触发，而非直接
- UI 绝不应阻塞游戏线程
- 可缩放文本和色盲模式是强制性的，而非可选的
- 在支持的最小和最大分辨率下测试所有界面
